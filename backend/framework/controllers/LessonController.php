<?php

include_once '../models/InviteModel.php';

$act = !empty($_GET['action']) ? $_GET['action'] : '';

switch ($act) {
    case 'value':
        # code...
        break;
    
    default:
        # code...
        break;
}